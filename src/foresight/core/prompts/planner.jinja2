You are a query planner. Your job is to produce a short action plan (max {{ max_steps }} steps) to answer the question below using only the available tools.

Question: {{ question }}

{% if working_memory %}
Working memory so far:
{% for chunk in working_memory %}
[{{ chunk.title }}] {{ chunk.text }}
{% endfor %}
{% else %}
Working memory: empty
{% endif %}

Available tools: retrieve, finish

---

Instructions:
1. Enumerate up to {{ beam_width }} candidate action sequences (each max {{ max_steps }} steps).
2. For each sequence, state the predicted post-state: which source titles should be in working memory after this sequence, and the minimum number of chunks expected.
3. Check preconditions for each step — eliminate any sequence where a step's precondition cannot be met given the prior steps.
4. Score each remaining sequence (0.0–1.0) for viability — how likely is this sequence to reach a correct answer?
5. Commit to the highest-scoring sequence.

Respond in JSON:
{
  "committed_plan": ["retrieve(<query1>)", "retrieve(<query2>)", "finish(<answer>)"],
  "predicted_post_state": {
    "required_sources": ["<title1>", "<title2>"],
    "min_chunks": 2
  },
  "viability_score": 0.0,
  "reasoning": "<brief explanation>"
}
